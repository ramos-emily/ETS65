from django.db import models

class Usuario(models.Model):
    bio = models.CharField( max_length=50)
    idade = models
    tell = models
    endereco = models
    escola = models
    animais = models
